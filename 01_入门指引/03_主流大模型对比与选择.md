# 第三章：主流大模型对比与选择

> 本章将介绍当前主流的大语言模型，帮助你了解不同模型的特点，并根据实际需求做出选择。

## 3.1 大模型分类

### 3.1.1 按开发者分类

| 类别 | 代表模型 | 特点 |
|------|----------|------|
| **OpenAI 系列** | GPT-4, GPT-3.5 | 能力最强闭源，API 生态完善 |
| **Meta 开源系列** | LLaMA 3, LLaMA 2 | 开源可商用，性能出色 |
| **Google 系列** | Gemini, PaLM | 多模态能力强 |
| **Anthropic 系列** | Claude 3 | 安全对齐好，长文本 |
| **中国厂商** | Qwen, ChatGLM, Kimi | 中文优化，开源友好 |

### 3.1.2 按功能分类

```
大语言模型
├── 通用大模型
│   ├── GPT-4 / GPT-4o
│   ├── Claude 3
│   ├── Gemini
│   └── LLaMA 3
│
├── 编程模型
│   ├── CodeLlama
│   ├── DeepSeek Coder
│   └── 通义灵码
│
├── 多模态模型
│   ├── GPT-4V
│   ├── Claude 3 (Vision)
│   ├── Gemini Vision
│   └── LLaVA
│
└── 垂直领域模型
    ├── 医疗：Med-PaLM
    ├── 法律：LegalBERT
    └── 金融：BloombergGPT
```

---

## 3.2 主流模型详细对比

### 3.2.1 GPT 系列

| 模型 | 发布时间 | 上下文 | 特点 | 适用场景 |
|------|----------|--------|------|----------|
| **GPT-4o** | 2024.05 | 128K | 最新多模态旗舰 | 高端任务 |
| **GPT-4 Turbo** | 2023.11 | 128K | 性价比高 | 日常开发 |
| **GPT-3.5 Turbo** | 2022.11 | 16K | 便宜快速 | 简单任务 |

```python
# OpenAI API 选择建议
if task_complexity == "high":
    model = "gpt-4o"  # 复杂推理、编程
elif task_complexity == "medium":
    model = "gpt-4-turbo"  # 对话、写作
else:
    model = "gpt-3.5-turbo"  # 简单问答、客服
```

### 3.2.2 Claude 系列

| 模型 | 发布时间 | 上下文 | 特点 | 适用场景 |
|------|----------|--------|------|----------|
| **Claude 3.5 Sonnet** | 2024.06 | 200K | 性价比最佳 | 通用任务 |
| **Claude 3 Opus** | 2024.03 | 200K | 最强能力 | 复杂推理 |
| **Claude 3 Haiku** | 2024.03 | 200K | 快速便宜 | 简单任务 |

> 💡 **提示**：Claude 在长文本处理和代码能力方面表现出色，特别适合处理长文档。

### 3.2.3 开源模型

| 模型 | 参数 | 特点 | 部署难度 |
|------|------|------|----------|
| **LLaMA 3 70B** | 70B | 开源可商用，性能接近 GPT-4 | 需要 GPU |
| **LLaMA 3 8B** | 8B | 消费级显卡可运行 | 中等 |
| **Qwen 2 72B** | 72B | 中文能力最强 | 需要 GPU |
| **Qwen 2 7B** | 7B | 消费级可运行 | 简单 |
| **ChatGLM 4** | 6B/130B | 中文对话友好 | 中等/难 |

```bash
# 本地部署 LLaMA 3 8B 推荐配置
# 最低：24GB 显存 (RTX 3090/4090)
# 推荐：32GB+ 显存

# 使用 transformers 加载
from transformers import AutoTokenizer, AutoModelForCausalLM

model = "meta-llama/Meta-Llama-3-8B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model)
model = AutoModelForCausalLM.from_pretrained(
    model, 
    device_map="auto",
    load_in_8bit=True  # 8位量化
)
```

---

## 3.3 如何选择合适的模型

### 3.3.1 选择决策树

```
需要选择模型?
│
├─ 是否需要本地部署?
│   │
│   ├─ 是 → 考虑硬件条件
│   │       │
│   │       ├─ 8GB+ VRAM → Qwen 2 7B / LLaMA 3 8B
│   │       │
│   │       ├─ 24GB+ VRAM → LLaMA 3 70B / Qwen 2 72B
│   │       │
│   │       └─ 80GB+ A100 → 完整精度模型
│   │
│   └─ 否 → 使用 API 服务
│
├─ 主要使用语言?
│   │
│   ├─ 中文 → Qwen 2 / ChatGLM / 智谱清言
│   │
│   └─ 英文 → GPT-4 / Claude 3 / LLaMA 3
│
├─ 是否需要多模态?
│   │
│   ├─ 是 → GPT-4V / Claude 3 Vision / Gemini
│   │
│   └─ 否 → 纯文本模型即可
│
└─ 预算考虑?
    │
    ├─ 免费/低成本 → 开源模型 + Ollama
    │
    ├─ 低预算 → GPT-3.5 / Claude Haiku
    │
    └─ 高预算 → GPT-4 / Claude Opus
```

### 3.3.2 场景推荐

| 场景 | 推荐模型 | 理由 |
|------|----------|------|
| **日常对话** | GPT-3.5 / Claude Haiku | 便宜、快速 |
| **代码开发** | GPT-4 / Claude 3 Opus | 编程能力强 |
| **长文档处理** | Claude 3 (200K 上下文) | 长文本优势 |
| **中文内容创作** | Qwen 2 / ChatGLM | 中文优化 |
| **本地部署学习** | LLaMA 3 8B / Qwen 2 7B | 消费级可跑 |
| **生产环境部署** | LLaMA 3 70B / Qwen 2 72B | 性能与成本平衡 |

### 3.3.3 成本对比

| 模型 | 输入价格 | 输出价格 | 1000次调用(平均) |
|------|----------|----------|------------------|
| GPT-4o | $5/1M | $15/1M | ~$10 |
| GPT-4 Turbo | $10/1M | $30/1M | ~$20 |
| GPT-3.5 Turbo | $0.5/1M | $1.5/1M | ~$1 |
| Claude 3.5 Sonnet | $3/1M | $15/1M | ~$9 |
| Claude 3 Haiku | $0.25/1M | $1.25/1M | ~$0.75 |

> 💡 **优化建议**：对于简单任务使用小模型，可以大幅降低成本。

---

## 3.4 API 对比与使用

### 3.4.1 主流 API 对比

| 提供商 | API 格式 | SDK 支持 | 国内访问 |
|--------|----------|----------|----------|
| **OpenAI** | OpenAI API | Python, Node.js | 需代理 |
| **Anthropic** | Claude API | Python, Node.js | 需代理 |
| **阿里云** | DashScope | Python, Java | 直接访问 |
| **智谱AI** | GLM API | Python, Node.js | 直接访问 |

### 3.4.2 OpenAI API 使用

```python
import openai

# 基础调用
response = openai.ChatCompletion.create(
    model="gpt-4",
    messages=[
        {"role": "system", "content": "你是一位专业助手"},
        {"role": "user", "content": "解释什么是机器学习"}
    ],
    temperature=0.7,
    max_tokens=500
)

# 获取回复
print(response.choices[0].message.content)
```

### 3.4.3 Claude API 使用

```python
import anthropic

client = anthropic.Anthropic(api_key="your-api-key")

response = client.messages.create(
    model="claude-3-5-sonnet-20241022",
    max_tokens=500,
    messages=[
        {"role": "user", "content": "解释什么是机器学习"}
    ]
)

print(response.content[0].text)
```

### 3.4.4 国内 API 使用

```python
# 阿里云 DashScope (Qwen)
from dashscope import Generation

response = Generation.call(
    model="qwen-turbo",
    prompt="解释什么是机器学习"
)

print(response.output.text)
```

---

## 3.5 本章小结

本章我们学习了：

1. **大模型的分类**：按开发者、按功能、按用途
2. **主流模型对比**：GPT、Claude、开源模型的特点
3. **模型选择策略**：根据场景、预算、语言选择合适的模型
4. **API 使用**：主流 API 的调用方式

---

## 📚 延伸阅读

- [OpenAI 模型定价](https://openai.com/pricing)
- [Anthropic Claude 定价](https://www.anthropic.com/pricing)
- [LLaMA 3 官方页面](https://ai.meta.com/llama/)
- [Qwen 2 开源页面](https://github.com/QwenLM/Qwen2)

---

*下一章我们将学习大模型的基础概念，深入理解 Token、Embedding 等核心术语。*
